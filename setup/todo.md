@see https://github.com/usernamehw/vscode-todo-md

`laravel new project-name` +setup
workspace settings #vs-code +setup
extensions #vs-code +setup
tasks #vs-code +setup
`git init --initial-branch=master` #git +setup
.gitattributes #git +setup
.gitignore #git +setup
eslint (config) +setup
prettier (config) +setup
typescript (config) +setup
vite (config) +setup
tailwind (config) +setup
.env +setup
husky +setup
linst-staged +setup
actions #github +setup
new repo #github +setup
auth #application +setup
wayfinder #application +setup
npm scripts #application +setup
